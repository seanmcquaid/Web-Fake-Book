import { memo } from 'react';
import styled from 'styled-components';

const Dropdown: React.FC = memo(() => <StyledDropdown></StyledDropdown>);

const StyledDropdown = styled.select``;

export default Dropdown;
